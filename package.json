{
  "name": "sanity-template-static-nextjs",
  "scripts": {
    "build": "sanity-template build --template-values dev/values.json",
    "watch": "sanity-template watch --template-values dev/values.json",
    "start": "cd build && yarn start"
  },
  "devDependencies": {
    "sanity-template": "2.3.1"
  }
}
